<?xml version="1.0" encoding="utf-8"?>
<!--
  Ruch jawny (bez TLS) dopuszczony WYŁĄCZNIE do adresów lokalnych używanych
  przy pracy z `wrangler dev`. Produkcyjny Worker chodzi po HTTPS i nie ma
  powodu, żeby ta lista kiedykolwiek urosła.

  Sama treść wiadomości jest zaszyfrowana MLS niezależnie od warstwy transportu,
  ale token dostępowy już nie — dlatego jawny HTTP nie może być domyślny.
-->
<network-security-config>
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
    </domain-config>
    <base-config cleartextTrafficPermitted="false" />
</network-security-config>
