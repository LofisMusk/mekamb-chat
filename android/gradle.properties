org.gradle.jvmargs=-Xmx3g -Dfile.encoding=UTF-8
org.gradle.parallel=true
org.gradle.caching=true

android.useAndroidX=true
android.nonTransitiveRClass=true

# Enabled parallel sync for Gradle 9.4+
org.gradle.tooling.parallel=true

# Backend domyślny dla tej gałęzi (dev): wskazuje testowego Workera z osłabionymi
# zabezpieczeniami (TRYB_DEWELOPERSKI). Dzięki temu `assembleDebug` bez -PapiUrl
# zapiecze w APK backend dev, nie produkcyjny. Na main tej linii nie ma.
apiUrl=https://mekamb-dev.grubyogon10.workers.dev
